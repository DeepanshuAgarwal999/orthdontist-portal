
import BlogList from './BlogList';

const Blogs = () => {
    return (
        <div className="py-12 bg-gray-50 min-h-screen">
            <BlogList />
        </div>
    );
};

export default Blogs;